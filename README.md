# Wine Cultivar Origin Prediction

This project trains a Random Forest classifier to predict wine cultivar from six chemical features and exposes a Flask web form.

Run training:

```bash
python model_building.py
```

Run app:

```bash
python app.py
```

Open http://127.0.0.1:5003
